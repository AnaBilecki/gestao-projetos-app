export interface Customer {
    id: number;
    name: string;
    email: string;
    phone: string;
    city: string;
    state: string;
}

export interface CustomerList {
    id: number;
    name: string;
}